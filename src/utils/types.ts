type Project = {
  title: string;
  description: string;
  imageUrl: string;
  techStack: string[];
  githubUrl: string;
  demoUrl?: string;
  liveUrl?: string;
  comingSoon?: boolean;
};

export type { Project };
